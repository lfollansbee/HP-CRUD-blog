<header>
  <div>
    <h1 class="title"><a class="home" href ="/">The Leaky Cauldron</a></h1>
    <p>Blog for Harry Potter musings.</p>
  </div>
</header>

<div class="edit">
  <h1>{{result.title}}</h1>

  <form action='/edit/{{result.id}}' method='post'>
    <label for="title">Post Title</label>
      <input type="text" name='title' value="{{result.title}}">
    <p>Originally written by: {{result.name}}</p>
    <!-- <label for="name">Username</label>
      <input type="text" name='name'> -->
    <label for="body">Post Body</label>
      <textarea rows="5" name="body" value="{{result.body}}">{{result.body}}</textarea>
    <label for='img'>Image URL</label>
      <input type="url" name="img" value="{{result.img}}">
    <button type="submit">Update</button>
  </form>
</div>
